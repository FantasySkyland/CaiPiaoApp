<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="mProgress_circle3">
        <item name="android:indeterminateDrawable">@drawable/progress_bar_waiting_circle3</item>
        <item name="android:duration">1000</item>
        <item name="android:minWidth">60dp</item>
        <item name="android:minHeight">60dp</item>
        <item name="android:maxWidth">80dp</item>
        <item name="android:maxHeight">80dp</item>
    </style>
</resources>
